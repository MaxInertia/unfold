<idea-plugin>
    <id>dev.unfold.ide</id>
    <name>Unfold</name>
    <vendor>maxinertia</vendor>
    <description>Inline call expansion in the editor: click a call to splice the callee body between the lines.</description>

    <depends>com.intellij.modules.platform</depends>
    <depends>org.jetbrains.plugins.go</depends>

    <extensions defaultExtensionNs="com.intellij">
        <applicationConfigurable instance="dev.unfold.ide.UnfoldConfigurable"
                                 displayName="Unfold"
                                 groupId="tools"
                                 id="dev.unfold.ide.settings"/>
    </extensions>

    <actions>
        <action id="unfold.ExpandCall"
                class="dev.unfold.ide.ExpandCallAction"
                text="Unfold: Expand Call"
                description="Splice the callee body below the current line.">
            <keyboard-shortcut keymap="$default" first-keystroke="control alt U"/>
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>
        </action>
        <action id="unfold.SelectRenderer"
                class="dev.unfold.ide.SelectRendererAction"
                text="Unfold: Select Renderer"
                description="Choose how expanded code is rendered.">
            <keyboard-shortcut keymap="$default" first-keystroke="control alt R"/>
        </action>
        <action id="unfold.FocusFrame"
                class="dev.unfold.ide.FocusFrameAction"
                text="Unfold: Focus Frame"
                description="Move focus down into the frame expanded on the current line.">
            <keyboard-shortcut keymap="$default" first-keystroke="control alt DOWN"/>
        </action>
        <action id="unfold.FocusParent"
                class="dev.unfold.ide.FocusParentAction"
                text="Unfold: Focus Parent"
                description="From inside a frame, move focus back to its call site.">
            <keyboard-shortcut keymap="$default" first-keystroke="control alt UP"/>
        </action>
        <action id="unfold.CollapseFrame"
                class="dev.unfold.ide.CollapseFrameAction"
                text="Unfold: Collapse Frame"
                description="Collapse the frame the caret is inside.">
            <keyboard-shortcut keymap="$default" first-keystroke="control alt BACK_SPACE"/>
        </action>
    </actions>
</idea-plugin>
